from tergite_autocalibration.config.package import ConfigurationPackage

if __name__ == '__main__':

    # Some function to get the number of qubits or the qubits themselves

    # Load the configuration package
    configuration_package = ConfigurationPackage()

    # Move the configuration package to a temporary location

    # Insert the template values

    # Rename the files inside the